// Se incluye la biblioteca estándar de entrada/salida para poder usar 'cout' y otras funciones
#include <iostream>

// Función principal del programa, punto de entrada de ejecución
int main() {

    // Se declaran e inicializan tres variables enteras: 'a' con 1, 'b' con 2 y 'c' con 3
    int a = 1, b = 2, c = 3;

    // Se crea un arreglo de enteros llamado 'arr' que contiene los valores de las variables 'a', 'b' y 'c'
    // El arreglo resultante será: [1, 2, 3]
    int arr[] = { a, b, c };

    // Se retorna 0 para indicar que el programa finalizó correctamente
    return 0;
}
