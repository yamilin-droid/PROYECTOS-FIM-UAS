# Se asignan los valores 1, 2 y 3 respectivamente a las variables 'a', 'b' y 'c' usando asignación múltiple
a, b, c = 1, 2, 3

# Se crea una lista llamada 'arr' que contiene los valores de las variables 'a', 'b' y 'c'
# El resultado será: [1, 2, 3]
arr = [a, b, c]
